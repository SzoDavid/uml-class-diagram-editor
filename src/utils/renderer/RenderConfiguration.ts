export interface RenderConfiguration {
    textSize: number,
    lineHeight: number,
    lineMargin: number,
    borderSize: number,
    fillColor: string,
    fillColorSelected: string,
    accentColor: string,
    accentColorSelected: string
}